---
# ansible-lint config for the standalone mosquitto role.
# Skips mirror the author's other roles: role-name (Galaxy allows the bare name)
# and schema (older-style task warnings, intentionally deferred).

skip_list:
  - role-name
  - schema
